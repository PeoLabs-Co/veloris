import React from 'react';

const StorefrontNavbar = () => {
  return (
    <header className="">
      {/* LUCY: Add Tailwind classes for a flex container, logo on the left, cart on the right */}
      <div>
        <span>Logo</span>
      </div>
      <div>
        <span>Cart</span>
      </div>
    </header>
  );
};

export default StorefrontNavbar;
